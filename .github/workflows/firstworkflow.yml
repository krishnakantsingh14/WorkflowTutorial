name: FirstWorkFlow

on:
  push:
    branches:
      - main  
  workflow_dispatch:
    

jobs:
  FirstJob:
    name: running a bash command
    runs-on: ubuntu-latest
    
    steps:
      - name: checkout
        uses: actions/checkout@v3
        
      - name: run a bash command
        run: |
          pwd
          echo "Username is ${GITHUB_ACTOR}"
  
